Given the source to an application which crashes when it is run. After running it ten times in a debugger, you find it never crashes
in the same place. The application is single threaded, and uses only the C standard library. What programming errors could be? How could
you test each one?

Random Variable

Uninitialized Variable (utilize runtime tool)

Memory leak (heap overflow, corruption of data on the stack)

External Dependencies